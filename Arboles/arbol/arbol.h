#ifndef Arbol
#define Arbol
template < class T > class arbol {
	private:
		typedef struct snode{
    			int data;
    			struct snode *left;
    			struct snode *right;
		}Nodo;
		//atributos y metodos auxiliares
		Nodo root;
		void insertHelper( Nodo ** ,const 

#endif
