#include "arbol.h"
#include <bits/stdc++.h>
using namespace std;


void init(Arbol &a,int){
 a.start=NULL;
 a.eder=NULL;
 a.eizq=NULL;
}
bool vacio(Arbol &a){
return a.start == null;
}

void inser(Arbol &a, int x){
    if(vacio(a))a.start->crearnodo(x);
    else{
        nodo *p;
        p=p->a.start;
        while(p!=NULL && x<)





    }
}
void print(Arbol &);
nodo *crearnodo(int);
